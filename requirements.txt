# cache-bust 2026-08-22: force fresh yt-dlp master pull, unpinned URL alone doesn't
# invalidate the build-layer cache since this file's bytes are what's hashed
flask
flask-cors
requests
yt-dlp[default] @ https://github.com/yt-dlp/yt-dlp/archive/refs/heads/master.tar.gz
static-ffmpeg
gunicorn
