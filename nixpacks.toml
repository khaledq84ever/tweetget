[phases.setup]
nixPkgs = ["python311", "ffmpeg", "git"]

[phases.install]
cmds = ["pip install --no-cache-dir -U -r requirements.txt"]

[start]
cmd = "gunicorn app:app --workers 2 --bind 0.0.0.0:$PORT --timeout 180"
